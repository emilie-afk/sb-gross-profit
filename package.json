{
  "name": "sb-gross-profit",
  "version": "1.1.0",
  "private": true,
  "description": "Succulents Box gross-profit dashboard and scenario calculator",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\" \"worker/test/*.test.mjs\"",
    "test:automation": "cd automation/shipstation-export && node --test test/*.test.mjs && cd ../shopify-export && node --test test/*.test.mjs",
    "build": "python3 build.py",
    "historical": "node tools/historical_totals.mjs",
    "hash-password": "node tools/hash-password.mjs",
    "backfill": "node tools/backfill.mjs"
  }
}
